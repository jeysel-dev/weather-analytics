# API (FastAPI) — spec 006. A versão de google-cloud-bigquery é FIXADA igual
# à de streamlit/requirements.txt: o acesso a dado (utils/bigquery.py
# adaptado) precisa se comportar exatamente como o já validado em produção
# no Streamlit — ex.: o tipo de retorno de max_date(). A conversão
# NUMERIC→float é feita manualmente em utils/bigquery.py (Decimal -> float),
# sem depender de pandas / db-dtypes / pyarrow — que a API não usa
# (query() devolve list[dict], não DataFrame).
fastapi==0.115.*
uvicorn[standard]==0.32.*
jinja2==3.1.*
google-cloud-bigquery==3.27.0
python-dotenv==1.2.2
