apiVersion: v1
kind: Service
metadata:
  name: weather-analytics
  labels:
    app: weather-analytics
spec:
  type: ClusterIP
  selector:
    app: weather-analytics
  ports:
    - name: http
      port: 8501
      targetPort: http
