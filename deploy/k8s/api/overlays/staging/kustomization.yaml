apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
- ../../base
- ingress.yaml

# Mantido pelo passo de CI (kustomize edit set image) a partir do 1º build.
images:
- name: ghcr.io/jeysel-dev/weather-analytics/api
  newName: ghcr.io/jeysel-dev/weather-analytics/api
  newTag: 9a7ca2e
