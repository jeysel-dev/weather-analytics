apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../../base
  - ingress.yaml

# Promoção manual (spec 090) — bump deliberado desta tag + commit, ou
# kubectl patch application weather-analytics-api-production -n argocd.
images:
  - name: ghcr.io/jeysel-dev/weather-analytics/api
    # Promovido manualmente em 2026-09-03 — 81fe368 (merge PR #1: mesoregion
    # vira macrorregião / crosswalk SAS-SC, ver CLAUDE.md; inclui também
    # dbe3ac2, filtro de dias unificado default 7 em
    # temperatura/precipitação/alertas — ainda não tinha sido promovido).
    # Produção rodava 7418b90 (spec 020, Tom Select) até este bump. Entrega
    # de uma vez:
    #   - dbe3ac2: filtro de dias unificado (default 7)
    #   - rótulos "Mesorregião" -> "Macrorregião" nas páginas
    #     temperatura/precipitação/alertas/comparativo/home (mudança
    #     visual; os dados em si só corrigem depois do runbook
    #     deploy/reconcile_mesoregion.sql rodar no pipeline/BigQuery — até
    #     lá a API segue servindo o mesoregion antigo do BigQuery, só com
    #     rótulo novo)
    # Build da imagem passou: pytest + smoke test /health (run 33777000465).
    #
    # Rollback: última versão boa em produção é 7418b90. `kustomize edit set
    # image ghcr.io/jeysel-dev/weather-analytics/api=...:7418b90` + commit +
    # sync.
    newTag: 81fe368
