apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../../base

# Promoção manual (spec 090) — bump deliberado desta tag + commit, ou
# kubectl patch application weather-analytics-api-production -n argocd.
images:
  - name: ghcr.io/jeysel-dev/weather-analytics/api
    # Promovido manualmente em 2026-09-02 — 82e4836 (migração completa:
    # as 6 páginas restantes — Temperatura, Precipitação, Alertas,
    # Cidades, Comparativo, Relatório por Cidade). Imagem validada em
    # staging com dado real de produção (BigQuery via chave da
    # weather-dashboard-sa — Opção B): as 7 rotas HTML respondem 200 com
    # header Server=uvicorn e menu de 7 itens; cada endpoint JSON das 6
    # páginas retornou linha real (rankings/tendência/anomalia,
    # ranking/intensidade/heatmap de chuva, resumo/por-tipo/municípios/
    # recentes de alertas, lista/clima/alertas de cidades, série/heatmap/
    # dia-vs-histórico do comparativo, dados do relatório); validação
    # nativa de parâmetro (422) e cidade inexistente (404) funcionando;
    # Streamlit sem regressão (/, /Temperatura etc. seguem em
    # TornadoServer). weather-analytics-api-staging Synced na promoção.
    # Tag anterior: c696a38 (só /horario).
    newTag: 82e4836
