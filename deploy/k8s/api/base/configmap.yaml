apiVersion: v1
kind: ConfigMap
metadata:
  name: weather-analytics-api-config
data:
  # Chave da SA dedicada da API (weather-analytics-api-sa, spec 090).
  GOOGLE_APPLICATION_CREDENTIALS_API: "/app/secrets/weather-analytics-api-sa-key.json"
