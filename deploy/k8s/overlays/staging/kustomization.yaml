apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
- ../../base
- ingress.yaml

    # Pull real confirmado na Fase 4 (spec 072) — a partir da Fase 5, este
    # valor passa a ser mantido pelo passo de CI (kustomize edit set image),
    # não editado à mão.
images:
- name: ghcr.io/jeysel-dev/weather-analytics
  newName: ghcr.io/jeysel-dev/weather-analytics
  newTag: cf5b614
